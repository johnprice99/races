{% for type, messages in app.session.flashBag.all %}
	{% for message in messages %}
		<div class="flash{{ type|capitalize }}">
			{{ message|trans({}, 'FOSUserBundle')|raw }}
		</div>
	{% endfor %}
{% endfor %}